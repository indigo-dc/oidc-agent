oidc-agent uses the MIT-licensed library 
[cJSON](https://github.com/DaveGamble/cJSON).

One can check for updates by using the following commands:
```
$ git clone https://github.com/DaveGamble/cJSON.git /tmp/cJSON
$ diff /tmp/cJSON/cJSON.c lib/cJSON/
$ diff /tmp/cJSON/cJSON.h lib/cJSON/
```
