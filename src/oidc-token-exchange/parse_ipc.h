#ifndef PARSE_IPC_EXCHANGE_H
#define PARSE_IPC_EXCHANGE_H

#include "oidc-token-exchange_options.h"

char* exchange_parseResponse(char* res, const struct arguments* arguments);

#endif  // PARSE_IPC_EXCHANGE_H
